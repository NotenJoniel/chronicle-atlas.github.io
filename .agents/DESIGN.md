# Chronicle Atlas — 設計思想・アーキテクチャ方針

> **このドキュメントの位置づけ**
> `BACKLOG.md` が「何をやるか（タスク）」を扱うのに対し、本書は「**なぜそう作るか（判断基準）**」を扱う。
> AIエージェントは、実装判断に迷ったとき・新しいタイムラインを追加するとき・既存コードを変更するときに本書を参照すること。
> タスクは消化されれば消えるが、本書の原則は残る。**原則と実装が食い違ったら、実装を直す。**

---

## 1. プロダクトの目的（不変）

**世界史のタイムラインを視覚的に分かりやすく表現し、「同じ時代に各地域で何が起きていたか」を比較しながら確認でき、歴史を楽しめるサイトをつくる。**

この一文が全ての判断の上位にある。以下は、そこから直接導かれる態度：

| 目的 | 導かれる態度 |
|---|---|
| **視覚的に分かりやすく** | 年表は「読む文書」ではなく「見る地図」。テキストの網羅性より、時間軸上の位置関係が一目で分かることを優先する |
| **同時代を比較できる** | 全タイムラインが**同一の時間軸**に載ること。これは機能ではなく前提条件 |
| **歴史を楽しめる** | 正確さは信頼の土台だが、目的ではない。逸話（`historyTrivia`）や人物のつながりが「面白さ」を担う |

> ⚠️ 「網羅的な歴史事典」を作ろうとしないこと。それは Wikipedia の仕事であり、本サイトは `wikiLinks` でそこに委譲している。
> 本サイトの価値は**選別と配置**にある。出来事を増やすより、時間軸上の見え方を良くする方が目的に近い。

---

## 2. North Star：横串ビュー（同時代比較）

最終到達点は、**複数文明を同一の時間軸に重ねて表示するビュー**である。

```
        BC500      BC250       AD1        AD250      AD500
中国  ├─戦国────────┤楚漢├─漢王朝──────────┤三国──┤
欧州  ├─共和政ローマ──────────┤ローマ帝国──────────────┤
日本                                                      … (AD1467〜)
```

- 個別の年表は他サイトにもある。**複数文明の同時代比較ができるサイトは希少**であり、ここがサイト名「Atlas」の意味そのもの。
- したがって横串ビューは「いつか作る機能の一つ」ではなく、**他の全ての設計判断が従属する目的地**である。

### 横串ビューが全ての設計に課す、たった一つの問い

> ### 🧭 **「そのタイムラインを、そのタイムラインの存在を知らないコードで描画できるか？」**

横串ビューは、7本（将来は10本以上）のタイムラインを**一つの汎用コード**で同時に描く。
つまり「共和政ローマ用の分岐」「三国志だけの特別扱い」がコードに残っている限り、横串ビューは実装できない。

**新しい実装・変更を加えるとき、この問いにYESと言えるかを必ず確認すること。**
NOになる変更（時代名や勢力IDをコードに直書きする等）は、たとえ短期的に動いても**目的地から遠ざかる負債**である。

---

## 3. 中心原理：3層モデル

上の問いを構造として表現したものが、以下の3層である。

```
┌─────────────────────────────────────────────────┐
│ ① データ層   data/timelines/*.json               │
│   その時代に固有の情報「すべて」                  │
│   出来事・人物・勢力・時代区分・勢力図            │
│   ★ 表示のための設定もここに含む（後述）          │
├─────────────────────────────────────────────────┤
│ ② 契約層     data/schema.json + validate.js      │
│   全時代が従う共通の型・語彙                      │
│   ここが守られている限り、③は①を知らなくてよい    │
├─────────────────────────────────────────────────┤
│ ③ 描画層     */app.js, */styles.css              │
│   時代を一切知らない汎用コード                    │
│   「データが来たらこう描く」だけを記述            │
└─────────────────────────────────────────────────┘
```

### 各層の責務と禁止事項

**① データ層 — 「時代に固有のものは、すべてここ」**
- 固有名詞、年、説明文はもちろん、**その時代特有の表示設定もデータである**
  - 勢力の表示順（サイドバーの並び）
  - 勢力の色
  - 勢力図の凡例ラベル
  - 初期表示する年
- 判定基準：**「他の時代では違う値になるもの」は、すべてデータ層に属する。**

**② 契約層 — 「時代をまたぐ約束事」**
- フィールド名（camelCase）、型、共通カテゴリ語彙を固定する
- `node data/validate.js` が唯一の正解。**人間のレビューを型チェックの代わりにしない**
- スキーマを変えるときは、7本すべてを同時に更新し、validate を通してからコミットする

**③ 描画層 — 「時代の名前が一つも出てこないコード」**
- `if (timeline === 'three-kingdoms')` のような分岐は**禁止**
- `const factionOrder = ['roma', 'populares', ...]` のような直書きも**禁止**（＝データ層に属する情報の漏出）
- CSSクラス名に時代固有の勢力名を焼き込まない（`.map-roma` ではなく、データ由来の色をインラインで与える）

---

## 4. 不変条件（AIが壊してはいけないもの）

以下は、破ると横串ビューが成立しなくなる。**変更提案そのものを拒否すること。**

### 4-1. 年は整数、紀元前は負数

```json
{ "year": -260 }   // ✅ BC260
{ "year": "BC260" } // ❌
```

- **理由**: これが横串ビューの背骨。全時代を1本の数直線に載せる唯一の手段。
- `"BC260"` `"紀元前260年"` などの表示文字列は、整数から**その場で導出する派生物**であり、データではない。
- `fmtYear()` / `fmtYearJa()` が変換を担当する。データ側に表示用文字列を持たせない。

### 4-2. 分類カテゴリは全時代共通の4つ

`battle` / `politics` / `episode` / `death`

- **理由**: 「政変だけ追う」「合戦だけ追う」を**全時代横断で**機能させるため。時代ごとに独自カテゴリを作った瞬間、横串フィルタは死ぬ。
- 増やしたくなったら、それは「7本すべてに意味があるか？」を問うこと。一つの時代のためだけのカテゴリは追加しない。
- `validate.js` が非標準カテゴリをエラーにする（ガードは既に効いている）。

### 4-3. 統一フィールド名（camelCase）

`eraPhases` / `factions` / `categories` / `characters` / `events` / `mapSnapshots` / `wikiLinks`

- **理由**: 横串ビューはビルド時に7本をマージするだけで成立する必要がある。フィールド名がずれると、マージのたびに変換表が必要になる。
- 🚨 **スキーマドリフトが最大の警戒対象。** 新規タイムライン追加時は、必ず `data/schema.json` と既存JSONを参照してから書き始める。

### 4-4. JSONはPretty-print（minify禁止）

- **理由**: diffが壊れると、AIによる部分編集ができなくなり、毎回ファイル全体の書き換えが必要になる（＝コスト爆発）。

### 4-5. Git運用

1. 作業は **`develop` ブランチ**
2. ブラウザで動作確認 → ユーザーに報告
3. **ユーザー承認後**に `main` へマージ（`main` = 本番 GitHub Pages）

> **絶対に未確認のコードを `main` にプッシュしない。**

---

## 5. コスト設計（AIが作り続けられる構造）

このサイトはAIが継続的に育てる前提で設計されている。**コンテキストコストは機能要件である。**

- **原則: AIのコストはファイル単位、機能の恩恵はスキーマ単位**
  三国時代を編集するとき読むのは `schema.json` + `three-kingdoms.json` だけ。他6本はコンテキストに入らない。
  それでいて全ファイルが同じ構造なので、横串ビューはマージするだけで成立する。
- **実数感**: 出来事1件 ≒ 250〜400トークン。1ファイル200〜300件を超えたら期間で分割する（例: `han-western.json` / `han-eastern.json`）。
- **横串ビューへの含意**: 7本の合計は既に約630KB。横串ビューが全ファイルを実データのまま読むのは筋が悪い。
  → **ビルド時に軽量ダイジェスト**（`year` / `title` / `category` / `timelineId` / 詳細ページへのアンカー）を生成し、横串ビューはそれだけを読む設計にすること。

---

## 6. 現在地（2026-08-08 実測）

### 実装済みタイムライン（20本）

| タイムライン | ディレクトリ | 期間 | 地域 | 出来事 | 人物 |
|---|---|---|---|---|---|
| 春秋時代 | `spring-autumn/` | BC770–BC476 | china | 41 | 31 |
| 中国戦国時代 | `warring-states/` | BC475–BC221 | china | 40 | 50 |
| 楚漢戦争 | `chu-han/` | BC209–BC202 | china | 27 | 36 |
| 漢王朝 | `han-dynasty/` | BC202–AD220 | china | 38 | 39 |
| 三国時代 | `three-kingdoms/` | AD184–AD280 | china | 54 | 91 |
| 古代ギリシア | `ancient-greece/` | BC800–BC323 | europe | 47 | 36 |
| ヘレニズム世界 | `hellenistic/` | BC336–BC30 | europe | 61 | 29 |
| 共和政ローマ | `roman-republic/` | BC509–BC27 | europe | 38 | 32 |
| ローマ帝国 | `roman-empire/` | BC27–AD476 | europe | 38 | 33 |
| 大航海時代・宗教改革 | `age-of-exploration/` | AD1453–AD1648 | europe | 56 | 43 |
| 律令国家の成立 | `asuka-nara/` | AD645–AD802 | japan | 49 | 40 |
| 摂関政治の時代 | `heian-court/` | AD894–AD1068 | japan | 36 | 31 |
| 源平合戦 | `genpei-war/` | AD1156–AD1189 | japan | 40 | 40 |
| 北条氏の台頭と承久の乱 | `hojo-rise/` | AD1199–AD1247 | japan | 36 | 34 |
| 元寇と鎌倉幕府の滅亡 | `mongol-invasion-japan/` | AD1268–AD1333 | japan | 38 | 29 |
| 南北朝の動乱 | `nanboku-cho/` | AD1334–AD1392 | japan | 38 | 26 |
| 日本の戦国時代 | `sengoku-japan/` | AD1467–AD1615 | japan | 72 | 46 |
| 幕末・維新 | `bakumatsu-ishin/` | AD1853–AD1877 | japan | 64 | 40 |
| モンゴル帝国 | `mongol-empire/` | AD1206–AD1368 | china | 60 | 41 |
| ジョチ・ウルス（キプチャク・ハン国） | `golden-horde/` | AD1237–AD1502 | europe | 58 | 27 |

**中国系（region: "china"、表示ラベルは「北・東アジア」）は`spring-autumn`の追加でBC770〜AD1368まで6本になった。欧州系はBC800〜AD1648まで6本。日本史は`asuka-nara`の追加で`heian-court`→`genpei-war`→`hojo-rise`→`mongol-invasion-japan`→`nanboku-cho`→`sengoku-japan`→`bakumatsu-ishin`の前に1本加わり計8本体制となった。**
→ `asuka-nara`（645–802）は`.agents/BACKLOG.md`「🔵 古代・平安の扱い」節に唯一残っていた未着手区間（2026-08-08実装）。BACKLOG原案の終点794年（平安京遷都）を、蝦夷との戦い（阿弖流爲の抵抗）に明確な決着をつけるため802年（阿弖流爲の降伏）まで8年延長した。これにより白村江の戦い・壬申の乱・藤原広嗣の乱・恵美押勝の乱・巣伏の戦い・蝦夷平定戦の6系統でBACKLOGが懸念した「`battle`カテゴリがほぼ空になる」問題を回避した。`factions`は蘇我氏／朝廷／藤原氏／橘氏／道鏡派／蝦夷／otherの7種。**勢力図は藤原氏→橘氏→（仲麻呂で藤原氏に復帰）→道鏡→天皇親政と政権の担い手が短期間で入れ替わりながら最終的に皇権に収斂し、最後の1枚だけ陸奥が朝廷の色に変わって蝦夷平定の決着を示す「政権担当者が回転しながら皇権に収斂する」新パターン**。`heian-court`の直前（日本史パート先頭）に配置したが、802〜894年の92年間（桓武朝後半〜平城・嵯峨朝、最澄・空海の開宗期）は接続せずスコープ外とした。
→ `heian-court`（894–1068）は日本史拡充ロードマップ完結後の追加コンテンツ（2026-08-08）。ユーザー提案の「平安時代」を、`.agents/BACKLOG.md`「🔵 古代・平安の扱い」節の既存方針通り「摂関政治の時代」として政治史ではなく人物・文化軸で実装した（平安時代400年をまるごと1本にすると中期の`battle`カテゴリが空になるため）。`characters[].field`の`arts`/`thought`が日本史で初めて主役級になり、`bunjin`（文化人）という他の日本史タイムラインに無い独立factionを新設した。**勢力図は藤原摂関家がほぼ全域を掌握し続ける中、最終スナップショットでのみ京・陸奥の2箇所を変化させ摂関政治の終わりの始まりを示唆する「安定の中に亀裂が入り始める」新パターン**。`genpei-war`の直前に配置したが、1068〜1156年（院政期）は接続せずスコープ外とした。
→ `spring-autumn`（BC770–BC476）は日本史ロードマップ完結後の次のコンテンツ拡充として追加（2026-08-08）。`warring-states`（戦国七雄、BC475–）の前史にあたり、両者を「春秋戦国時代」として統合しなかった理由は`.agents/BACKLOG.md`「中国史拡充: 春秋時代」節を参照（プレイヤー総入れ替え・明確な境界事件・規模・fieldの活用の4点）。実装にあたり`warring-states.json`から孫武を移設し、孫武と統合されていた出来事を墨子単独の内容に分割、三家分晋イベントに戦国開始年の複数学説（BC475/453/403）を明記した。**勢力図は最終的な単一勢力への統一を迎えず、晋・斉・楚・秦・周・鄭が並立したまま`warring-states`の多極的な合従連衡へ引き継ぐ、「統一」でも「崩壊」でもない新しいパターン**（呉滅亡時のみ、呉のマスを削除せず越の色に塗り替える§10-1の原則を適用）。
→ `genpei-war`は`.agents/BACKLOG.md`「日本史拡充ロードマップ」の第一弾（2026-08-07）。保元の乱〜奥州藤原氏滅亡の33年間を時代区分（平安/鎌倉）で割らず一本化した。
→ `hojo-rise`（1199–1247）はロードマップ第二弾（2026-08-08）。`genpei-war`の直後から始まり年代的な空白なく接続する、日本史パート初の「切れ目のない連続2本」。源頼朝・北条政子・時政・義時・梶原景時・大江広元は`genpei-war`と同一人物としてIDを再利用（`c_alexander_the_great`等と同じ境界人物の重複方式）。§10-1の「勢力図は位置を`mapLayout`で明示し、滅亡勢力は色を塗り替える」原則を最初から適用した最初のタイムライン。
→ `mongol-invasion-japan`（1268–1333）はロードマップ第三弾（2026-08-08）。`hojo-rise`の直後から接続し、日本史パートが3本連続になった。クビライ・北条時宗は既存の`mongol-empire.json`と同一人物としてID（`c_kublai`/`c_hojo_tokimune`）を再利用し、元寇を元側・日本側の両視点から見られるようにした。**勢力図は北条方が最終的に盤面から完全に消える「崩壊」型として設計した初のタイムライン**（従来の`hojo-rise`/`warring-states`のような「一つの勢力への統一」とは逆方向の応用）。
→ `nanboku-cho`（1334–1392）はロードマップ第四弾（2026-08-08）。`mongol-invasion-japan`の直後から接続し、1156〜1392年の236年間が切れ目なく続く日本史パートが完成した。勢力図は「崩壊」型から「統一」型に回帰。
→ `bakumatsu-ishin`（1853–1877）はロードマップ最終弾（2026-08-08）。日本史拡充ロードマップ全5本がこれで完結した。既存4本（1156–1392年）とは461年の空白があり、`sengoku-japan`（1467–1615）とも直接接続しない独立した単発タイムライン。ユーザーへの確認の上、BACKLOGの原案通り1853–1877年を1本にまとめる方針で実装（1868年の明治維新を境に2分割する案もあったが不採用）。12章・64件・40人と`sengoku-japan`に匹敵する最大級の規模。**勢力図（会津・長州・京都・江戸・薩摩）は1869年の戊辰戦争終結でいったん`meiji_gov`に統一された後、1877年の西南戦争（薩摩の一時的離反）を経て再統一される「統一→動揺→再統一」の3段構成**で設計し、単純な統一型・崩壊型とは異なる新しいパターンを追加した。
→ `nanboku-cho`（1334–1392）はロードマップ第四弾（2026-08-08）。`mongol-invasion-japan`の直後（1333年の「建武の新政、始まる」の翌年）から接続し、1156〜1392年の236年間が切れ目なく続く日本史パートが完成した。後醍醐天皇・楠木正成・新田義貞・足利尊氏・千種忠顕は`mongol-invasion-japan`と同一人物としてIDを再利用。**勢力図は`mongol-invasion-japan`の「崩壊」型から一転、南朝方（紫）が最終的に足利方（金）に統一される`hojo-rise`/`warring-states`と同型の「統一」パターンに回帰**（南北朝合体という史実自体が統一の物語であるため）。次点は「幕末・維新」（1853–1877、日本史パートの最終候補）。
→ `golden-horde`は`mongol-empire`のバトゥ西征（1235–1242年）の続きとして、ヨーロッパ側に根を張り続けた後継国家の265年間を単独タイムライン化したもの。`age-of-exploration`とAD1453〜1502年で重なり、欧州系タイムラインの並行本数が初めて2本になる区間ができた。
→ `age-of-exploration`の追加により、それまで孤立していた`sengoku-japan`（AD1467〜）が欧州系タイムラインとAD1453〜1615の範囲で重なるようになった。横串ビューで日本・中国・ヨーロッパの3地域が同時に意味のある形で並ぶ状態が実現している。
→ `mongol-empire`は当初 `region: "others"` で実装したが、統治の中心（大ハーン位・元朝の帝都）が一貫して中国方面にあるため`region: "china"`に収め、地域ラベルを「中国」から「北・東アジア」へ改称した（`region` idは`china`のまま。既存4本への変更は不要）。region判定の詳しい経緯は `.agents/BACKLOG.md` のコラム参照。

### 層ごとの達成度

| 層 | 状態 | 評価 |
|---|---|---|
| ① データ層 | 7本すべて統一スキーマ、`validate.js` 全パス（0 errors / 2 warnings=意図的なID重複） | ✅ **横串ビューの前提を満たしている** |
| ② 契約層 | `schema.json` + `validate.js` が機能。カテゴリ4種の強制、meta必須項目、色形式、mapLayout整合性も検証済み | ✅ 良好 |
| ③ 描画層 | `shared/timeline-core.js` + `shared/timeline-core.css` に統合済み。タイムラインIDでの分岐なし | ✅ **G3完了(2026-08-05)** |

---

## 7. 横串ビューまでのギャップ（優先度順）

現状、**データ層・描画層ともに準備完了。G1〜G4・G7まで完了し、横串ビュー（俯瞰・詳細比較の両レベル）は実証済み。残る課題はG5〜G6とコンテンツ拡充。**

### ✅ G1. IDのグローバル一意化 【対応済み 2026-08-05】

```
DUP event: e_actium      … roman-republic.json と roman-empire.json の両方に存在
DUP char:  c_cleopatra   … 同上
```

- **なぜ致命的か**: 横串ビューは7本をマージする。IDが衝突すると、どちらかが静かに消えるか上書きされる。
- 上記2件は内容を確認済み。事故的コピペではなく、**意図的な重複**（BC31年のアクティウムの海戦とクレオパトラは、共和政の終わりと帝政の始まり両方の物語の境界イベントとして、別内容で記述されている）。したがってデータの削除・統合はしない。
- `validate.js` に**横断重複チェックを追加済み**（`node data/validate.js` 実行時、`📋 Cross-file ID uniqueness` セクションに warning として出力される。境界年代の意図的な重複が正当なケースのため、エラーではなく警告扱い）。
- **横串マージ時の名前空間化規約**: 横串ビュー等でタイムラインをマージする処理を実装する際は、キーを必ず `${timelineId}::${localId}`（例: `roman-empire::e_actium`）の形で名前空間化すること。個別ファイル内のIDはそのまま維持し、マージ層でのみ変換する。JSON内部のIDを `roman-empire/e_actium` のようにファイル側で書き換える必要はない。

### ✅ G2. 時代固有の設定をコードからデータへ 【完了 2026-08-05】

`app.js` / `styles.css` に埋まっていた「データであるべきもの」はすべてデータ層に移した：

| 埋まっていたもの | 移行先 |
|---|---|
| 勢力の表示順 `factionOrder` | `factions{}` のJSON宣言順から導出 |
| 勢力図の凡例ラベル | `factions[].name` |
| 初期表示年 `renderMap(-200)` | `mapSnapshots[0].year` |
| 勢力の色（`.map-roma` 等のCSSクラス） | `factions[].color` / `colorLight`（JSがインラインstyleで適用） |
| three-kingdomsの地図固定レイアウト（`MAP_LAYOUT`） | `meta.mapLayout` |
| 地図の列数計算式（3パターンあった） | `meta.mapColumnRules` |
| 旧世代3本の静的フィルタボタン（`index.html`直書き） | 全7本で`renderFactionFilters()`/`renderCategoryFilters()`による動的生成に統一 |
| ヘッダー文言・検索placeholder・モーダル見出し等 | `meta.title`/`headerTitle`/`searchPlaceholder`/`descLabel`等 |

地図専用の疑似勢力（董卓・袁紹・劉表・劉璋・係争地など）は `mapOnly:true` フラグ付きで `factions{}` に正式登録済み（G1/G2の時点で対応済み）。G3の共通コードはこのデータをそのまま活用し、`renderFactionFilters` では `mapOnly` 勢力を除外して勢力フィルタボタンには出さないようにしている。

### ✅ G3. 描画層の重複解消 【完了 2026-08-05】

7本の `app.js`（各527〜557行、実質95%同一）と `styles.css` を統合した。

- **`shared/timeline-core.js`**: 全7本を駆動する単一エンジン。`body[data-timeline]` からタイムラインIDを読み、`data/timelines/<id>.json` を fetch して描画する。**タイムラインIDによる分岐は一切持たない** — `meta.mapLayout`/`mapColumnRules`/`historyDiff`/`historyOnly` 等、データの有無だけで挙動が変わる設計（③の判断基準を満たす）
- **`shared/timeline-core.css`**: 構造・レイアウト・モーダル・アニメーションを統一。ただし `.timeline`/`.event-card` の見た目（rail型＝左レール+ドット／flat型＝フラットカード）は `body[data-layout]` で切り替える2バリアントとして両方保持（時代ごとの意図的な個性として維持、統一しない）
- **`<name>/theme.css`**: 各タイムライン固有の配色（`--bg`/`--gold`等の基本テーマ変数）のみを残す。勢力色はテーマ変数ではなくJSONの`factions[].color`から直接インラインstyleで適用するため、`.map-<fid>`/`.tag-<fid>`というCSS変数駆動クラスは全廃した
- 各 `<name>/app.js` は削除。`<name>/index.html` が `shared/timeline-core.js` を直接読み込む
- three-kingdoms固有だった「正史/演義」表示機能（`historyOnly`バッジ、`historyDiff`セクション）は、特定タイムラインへの分岐ではなく「データにあれば汎用的に表示する」オプション機能として実装 — 将来他タイムラインでも使える
- 移行に伴い、three-kingdoms.jsonのevents配列にあった3箇所の年代順崩れ（ソート処理が無かったため）と、`syncActiveEvent`のyear=0無反応バグを修正
- Playwrightで7本すべてを検証（初期表示・フィルタ・検索・モーダル・地図・レイアウトバリアント・historyOnly/historyDiff）、コンソールエラー無し、イベント件数もデータと完全一致

### ✅ G4. 詳細比較モード 【完了 2026-08-05、🗑️ 2026-08-06 削除（G7に役割統合）】

> `compare/` は G7（横串ビュー）の追加後、「同時代比較」の役割が地域別俯瞰ビューに実質的に統合されたため削除した。以下は実装当時の設計記録として残す（レーン方式の衝突回避など、`cross/cross.js` が踏襲しているロジックの由来）。

当初は「`data/digest.json` をビルド時生成し、全7本を実データのまま読ませない」という設計を想定していたが、実装検討の結果**digest.jsonは不要と判明**した。詳細比較は2〜4個のタイムラインしか同時に扱わないため、選択したタイムラインの完全なJSON（各60〜130KB）を直接fetchすれば十分（説明文・人物情報等、比較に必要な情報も揃う）。トップページ全体（7本すべて・紀元前800年〜西暦1920年）への出来事分布表示のような将来拡張が出てきた場合に、その時点で改めてdigest.jsonの必要性を検討する。

- **`compare/index.html` + `compare.css` + `compare.js`** を新規作成。`compare/index.html?t=<id1>,<id2>[,<id3>,<id4>]` で2〜4個のタイムラインを選び、「列=タイムライン、縦軸=年」という共通の視覚言語（トップページの `.wt-grid`/`yearToY()` と同じ思想）で出来事を並べる
- 年軸は選択したタイムラインの**和集合**（積集合ではない — 一部期間が軸から消えて「何も無かった」と誤読させることを防ぐ）。重なり区間はハイライト帯で明示し、重ならない組み合わせは警告バナーを出す（ブロックはしない）
- 出来事は縮小カードを年座標に絶対配置する。**当初は貪欲法による縦の衝突回避+コネクタ線を採用していたが、密度が高い時代（短期間に出来事が集中）で縦位置が実際の年から際限なくずれていく問題が発覚し、レーン方式（同時期に重なる出来事はカレンダーアプリの予定表示のように横に並べる、最大4レーン）に変更した。これにより出来事は常に実際の年に一致した縦位置で描画され、密集は横方向の狭まりとして吸収される**
- `shared/timeline-core.js` に**1関数のみ追加**（`handleDeepLink()`、URLハッシュ `#e_xxx`/`#c_xxx` を見て該当モーダルを自動オープン）。既存7本の動作には一切影響しない。比較ビューの「元の年表で見る」ボタンがこれを使う
- モーダル等のCSSは `shared/timeline-core.css` をそのまま再利用。JSロジックは複製（`shared/`への抽出はYAGNIにより見送り）
- トップページの `nav-hint` バーに「🔀 同時代比較」への導線を追加

**出来事へのアンカーリンク**（`#e_xxx`）は上記の`handleDeepLink()`で実現済み。SEO向けの静的HTML化（BACKLOG項目9）は別課題として残る。

### ✅ G7. トップページ拡張（横串ビュー・地域別俯瞰） 【完了 2026-08-06】

DESIGN.md §7 冒頭で「①トップページの拡張（俯瞰レベル）」として未着手扱いだったものを、既存の `index.html` を一切変更せず**別ページ**として実装した。

- `cross/index.html` + `cross.css` + `cross.js` を新規作成。列＝地域（日本／中国／ヨーロッパ／その他、`index.html` の `REGIONS` と同じ4分類）固定で、各地域列の中にその地域へ属する全タイムラインの出来事を年代でつなげて表示する。`compare/`（列＝選択した2〜4タイムライン）と対になる俯瞰ビュー
- 実データ全件（9本・415件）をそのままfetchすると重いため、`data/build-digest.js` を新設し `data/digest.json`（出来事の年・タイトル・カテゴリ・場所・勢力色、人物一覧用の軽量な人物/勢力データ、共通カテゴリ定義）を生成する運用に切り替えた。§5で想定していた「トップページ全体への出来事分布表示のような将来拡張が出てきた場合にdigest.jsonの必要性を検討する」が現実になったケース。**タイムラインデータを編集したら `node data/build-digest.js` を再実行してコミットすること**
- 初期描画は digest.json のみで完結し、出来事/人物カードをクリックした時だけ該当タイムラインの実JSONを遅延fetch（`compare/compare.js` の `loadOneTimeline` と同じキャッシュ付きパターン）してモーダル（同じく `compare/compare.js` のモーダル描画を複製）を開く2段構成
- 年軸・レーン配置（`layoutCards`）は `compare/compare.js` から複製。地域列は複数タイムラインの出来事を1本の配列として結合してからレーン計算に渡すことで、タイムラインをまたいでも衝突回避が機能する。`compare/compare.js` の密度追従型 `computeDensityPxPerYear` はここでは不採用（9本の合計415件を1本の年軸に載せると、密度に追従させ続けてもページ全体が実測10万px超まで際限なく伸びるだけで解消しないため）。全域スパンに対して妥当な総高さになる固定係数のみを使い、収まりきらない密集はレーン方式と縦位置のわずかなズレで吸収する（正確な位置は個別タイムラインや②詳細比較モードに委ねる）
- 新規の「タイムラインOR絞り込み」（地域列ヘッダーの折り畳みチェックボックス、初期値は全ON）を追加。フィルタ操作（タイムラインOR・カテゴリ・検索）は軸やレーン配置を一切再計算せず、`.dimmed`（opacity）の付け外しのみで見た目を変える（`compare/compare.js` の `applyFilters` と同じ方式）。これにより地域列をまたいだ年軸の位置ズレ＝横串ビューの意義そのものがフィルタ操作中も崩れない
- タイムラインの切れ目は「時代の枠」（トップページのera-blockと同じ、どこからどこまでがその時代かを示す背景の帯 + スクロール追従ラベル）として表示する。色は時代固有のハードコードをせず交互の濃淡のみで隣接区分を分ける。実データ上タイムラインの期間が重なる場合（ヨーロッパ＝古代ギリシア/ヘレニズムと共和政ローマ/ローマ帝国が並行、中国＝三国時代が後漢末期(184〜220年)と重なる）は帯を素直に重ねて描き、ラベルだけ「開始時点で空いている最も直近に終わったレーン」を選ぶ貪欲法（lineageのような時代固有のハードコードなし）で横にずらして衝突を避ける
- 人物一覧は各地域列の中には作らず、詳細タイムラインと同じ「画面左に1つだけ」のサイドバー（`shared/timeline-core.css` の `.main-layout`/`.sidebar` をそのまま流用）に統一。中身は地域→時代の階層で並べ、時代を開くと詳細タイムラインのサイドバー（`renderSidebar`）と見た目・機能を完全に揃えた人物一覧（折りたたみ可能なグループ見出し）を表示する。勢力順/分野順トグルと分野フィルタは時代ごとには置かず、詳細タイムラインと同じくサイドバー全体で1つ（`personState`）。人物クリックで選択トグル→該当人物が関わる出来事だけに全地域列を絞り込み（`selectedChars`、詳細タイムラインと同じ動作。人物IDがタイムラインをまたいで重複しうるため `${timelineId}::${charId}` で名前空間化）、Ctrl+クリック/右クリックで人物モーダルを開く点も詳細タイムラインと同じ。ヘッダーの「👥 人物一覧」ボタンでサイドバー全体を開閉できる点も `btn-toggle-sidebar` と同じ操作感に揃えた。「時代を選択」でチェックを外した時代は、サイドバー側の該当エントリも連動して隠れる（開閉状態は `openEraIds` でフィルタ変更後の再描画をまたいで保持）
- `index.html` への変更は `.nav-hint` に `🧭 横串ビュー` リンクを1行追加しただけ。年表描画ロジック自体は無変更

### 🟡 G5. `certainty` が宣言のみで未実装

- `schema.json` に定義済みだが、**全7本で使用0件、`app.js` からの参照も0件**。
- 諸説ある年代（例: 北条早雲の伊豆討ち入り 1491年説 / 1493年説）を点線等で区別すると、歴史好きからの信頼度が上がる。
- 横串ビューでは「この線はどれくらい確かか」の表現が個別年表より重要になる。

### 🟡 G6. タイムライン一覧の二重管理

- トップ `index.html` が時代一覧を**JS配列で直書き**しており、`data/index.json` と二重管理になっている。
- 片方だけ更新される事故が起きる。`index.json` を単一の情報源にする。

---

## 8. コンテンツ拡充の方針

**「出来事を増やす」より「時間軸の重なりを増やす」方が目的に近い。**
横串ビューの価値は、同じ時代に複数地域が並ぶことで生まれる。したがって拡充の優先度は「**既存タイムラインと時期が重なるか**」で決まる。

| 候補 | 重なる相手 | 状態 |
|---|---|---|
| **ヘレニズム時代**（アレクサンドロス） | 中国戦国時代・共和政ローマ | ✅ 実装済み（`hellenistic`） |
| **大航海時代・宗教改革** | 日本の戦国時代 | ✅ 実装済み（`age-of-exploration`、2026-08-06）。ザビエル来日（1549年）で`sengoku-japan`と交差する初のタイムライン |
| **モンゴル帝国** | 元寇で日本、レグニツァ/モヒの戦いでヨーロッパ、大越の抵抗で東南アジアと接続 | ✅ 実装済み（`mongol-empire`、2026-08-06）。`region: "china"`（「北・東アジア」列）に配置し、中国系タイムラインをBC475〜AD1368まで5本に拡張 |
| **ジョチ・ウルス（キプチャク・ハン国）** | `mongol-empire`のバトゥ西征と地続き、`age-of-exploration`とAD1453〜1502年で重なる | ✅ 実装済み（`golden-horde`、2026-08-06）。`region: "europe"`に配置し、欧州系タイムラインをBC800〜AD1648（一部区間は2本並行）に拡張 |

> `sengoku-japan`（AD1467〜）は`age-of-exploration`（1453〜1648）が加わったことで、他と800年以上離れた「単発の島」ではなくなった。横串ビューで日本列とヨーロッパ列が初めて同じ年軸上に実際の出来事として重なる（例: 1549年ザビエル来日 と 同年代のヨーロッパの宗教改革の動き）。

### 8-1. タイムラインの区切り方：暦の時代区分ではなく物語の単位（2026-08-06 方針決定）

新しいタイムラインの範囲を決めるとき、教科書的な時代区分（王朝・世紀・元号など）をそのまま採用しないこと。`sengoku-japan`（応仁の乱→天下統一）が実証しているように、**起承転結のある物語の単位**で区切ったタイムラインの方が、出来事の密度が均一になり、`battle`/`politics`/`episode`/`death`の4カテゴリもバランスよく埋まる。

- 逆に、政変・制度整備が中心で軍事的な駆動力が弱い区間（例: 日本史の飛鳥・奈良、平安中期）を暦年代でそのまま切り出すと、`battle`カテゴリがほぼ空になるなど密度が偏る。そうした区間は時系列区分ではなく**テーマ軸**（制度史・文化史）で一本にまとめることを検討する
- 判断基準：「この区間を、起承転結のある一つの物語として要約できるか？」に**Yes**と言えない場合、区切り方を疑うこと
- 詳細な候補と優先順位は `.agents/BACKLOG.md`「日本史拡充ロードマップ」を参照

---

## 9. 判断に迷ったときの原則

新しい実装・変更に際して、上から順に適用する。

1. **横串ビューに近づくか？** 遠ざかる変更は、動いてもマージしない。
2. **時代を知らないコードで描けるか？** NOなら、時代固有の部分をデータ層へ動かす。
3. **他の時代では違う値になるか？** YESならそれはデータであり、コードに書いてはいけない。
4. **7本すべてに適用できるか？** 1本のための特別扱いは、7本目の特別扱いを呼ぶ。
5. **`validate.js` は通るか？** 通らないものはコミットしない。ルールを増やしたときは検証も増やす。
6. **次に触るAIは、このファイルだけ読めば分かるか？** 暗黙知はコンテキストコストとして跳ね返る。

---

## 10. 新規タイムライン追加の手順

1. `data/schema.json` と**既存の近い時代のJSON**を読む（例: 欧州なら `roman-republic.json`）
2. `data/timelines/<id>.json` を統一スキーマで作成
   - カテゴリは `battle` / `politics` / `episode` / `death` の4つのみ
   - 年は整数、紀元前は負数
   - IDは既存タイムラインと衝突しないこと（§7 G1）
   - `characters[].field`（統治/軍事/行政/思想/学術/芸術/経済の機能7分類＋公的機能を持たない人物用の`other`、必須）を全員に付与する。判断基準は `.agents/FIELD_TAXONOMY.md` を参照
3. `data/index.json` に登録（`id` / `name` / `region` / `startYear` / `endYear` / `dataFile` / `subProjectDir`）
4. `node data/validate.js` を実行し、**0 errors** を確認
5. サブプロジェクトのディレクトリを作成（既存プロジェクトをテンプレートとして参照）
6. トップ `index.html` にリンクを追加（`link` / `linked:true`）
7. `develop` にコミット → ブラウザで確認 → **ユーザー承認後**に `main`

### 10-1. `mapSnapshots`（勢力図）を作るときの2原則【2026-08-07 追記】

12本の勢力図を実地理と照合したところ、多くが `territories` の記述順＝動的グリッドの並び順（`meta.mapColumnRules` の列数で機械的に折り返すだけ）になっており、実際の地理的位置関係と無関係だった。今後は以下を徹底する。

**① 位置は `meta.mapLayout` で明示指定する。** `territories` のオブジェクト順に暗黙で頼らない（読む人が「なぜこの並び順か」を書いた本人以外に説明できない）。`mapLayout` は行×列の2次元配列（`null`で空白セル）で、対象地域の実際の地理（北→南を行、西→東を列に対応させるのが基本形）に合わせて設計する。`three-kingdoms.json`（中国13州）が最初の実装例、`warring-states.json`／`chu-han.json`／`han-dynasty.json`／`ancient-greece.json`／`hellenistic.json`／`age-of-exploration.json`／`mongol-empire.json`／`golden-horde.json`／`genpei-war.json` も同方式。`roman-republic.json`／`roman-empire.json`／`sengoku-japan.json` は`_e1`等の空白ダミーIDを使ったダミー行埋め＋一定の列数運用で、結果的に西→東の並びが保たれている実装済み例（新規追加時は素直に`mapLayout`を使う方が保守しやすい）。

**② 勢力が滅んだら、そのマスは消さずに征服した勢力の色へ塗り替える。** 同じ領土IDを全スナップショットに残したまま `faction`（と`lord`）だけを征服者のものへ更新する。**マスを`territories`から削除して空白にしない。** 削除すると「負けた勢力から消えていき、最後に小さな1マスだけ残る」形になり、統一の広がりが伝わらない（実際の面積拡大を表現できないため）。`mongol-empire.json`／`sengoku-japan.json`／`three-kingdoms.json` は最初からこの方式で正しく実装されていた。`warring-states.json`（戦国七雄→秦の統一）と`chu-han.json`（群雄割拠→漢の統一）は、当初この原則に反して消滅した勢力のマスを削除していたため、2026-08-07に全滅亡国を征服者の色へ塗り替える形に修正した。

---

*最終更新: 2026-08-08 — 新規タイムライン「律令国家の成立」（`asuka-nara`、645–802年）を追加。`heian-court`の前史として日本史を8本に拡充し、BACKLOGに唯一残っていた飛鳥・奈良の空白区間を埋めた。終点を794年から802年（阿弖流爲の降伏）へ延長し`battle`カテゴリの薄さを解消。勢力図に「政権担当者が回転しながら皇権に収斂する」という新パターンを追加。追記：万葉集・風土記・懐風藻を記紀と同じ「国家事業」の枠組みで追加し、柿本人麻呂・大伴家持（field: arts）を新規人物として追加した*
